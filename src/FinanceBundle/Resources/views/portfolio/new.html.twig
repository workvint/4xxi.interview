{% extends 'AppBundle:home:index.html.twig' %}

{% block body %}
    <h1>Portfolio creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="{{ 'portfolio.form.button.new'|trans }}" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('portfolio_index') }}">{{ 'portfolio.form.list.label'|trans }}</a>
        </li>
    </ul>
{% endblock %}
