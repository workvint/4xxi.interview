{% extends 'AppBundle:home:index.html.twig' %}

{% block body %}
    <ul class="breadcrumb">
        <li>
            <a href="{{ path('portfolio_index') }}">&larr;{{ 'portfolio.new.list.label'|trans }}</a>
        </li>
    </ul>
        
    <h1>{{ 'portfolio.new.h1'|trans }}</h1>

    <div class="panel panel-default">
        <div class="panel-body">    
    
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="{{ 'portfolio.new.form.submit'|trans }}" class="btn btn-success" />
        {{ form_end(form) }}
      
        </div>  
    </div>
{% endblock %}
